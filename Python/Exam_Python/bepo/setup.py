from setuptools import setup, find_packages

setup(
    name = "bepo",
    version = " 1.1.1",
    py_modules = ["bepo"],
    author = "exam",
    url = "dkdkdkd",
    description = "a sample printer of nested lists",
)
